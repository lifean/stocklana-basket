import Link from 'next/link';
import { baskets } from '@/lib/baskets';
export default function Home() {
  return <main><section className="hero"><span className="eyebrow"><span className="status-dot" /> EQUITIES, MEET SOLANA</span><h1>Build your portfolio.<br /><span>Own the stocks directly<br className="desktop-break" /> in your wallet.</span></h1><p>Create diversified exposure to tokenized equities in seconds.</p><a className="button primary" href="#strategies">Explore strategies <span>↗</span></a><div className="hero-notes"><span>01 &nbsp; Choose a strategy</span><span>02 &nbsp; Set your amount</span><span>03 &nbsp; Preview your allocation</span></div></section>
    <section id="strategies" className="strategies"><div className="section-heading"><div><span className="eyebrow">A SIMPLE PLACE TO START</span><h2>Three strategies. Your call.</h2></div><p className="muted">Curated allocations.<br />Individual tokens in your wallet.</p></div><div className="cards">{baskets.map((basket, i) => <article className={`strategy-card strategy-${i}`} key={basket.id}><div className="card-top"><span className="emoji">{basket.emoji}</span><span className="tag">{basket.assets.length} assets</span></div><h3>{basket.name}</h3><p className="muted card-description">{basket.description}</p><div className="allocation-bar" aria-hidden="true">{basket.assets.map((a, j) => <span key={a.symbol} className={`segment segment-${j}`} style={{ width: `${a.weightBps / 100}%` }} />)}</div><dl className="allocations">{basket.assets.map(a => <div key={a.symbol}><dt>{a.stockSymbol}</dt><dd>{a.weightBps / 100}<span className="muted">%</span></dd></div>)}</dl><Link className={`button ${i === 0 ? 'primary' : 'secondary'} full`} href={`/basket/${basket.id}`}>Invest <span>↗</span></Link></article>)}</div></section><p className="disclaimer">Tokenized equities carry market and issuer risk. This project is a hackathon demo and does not constitute investment advice.</p></main>;
}
